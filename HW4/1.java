

class Sortit {
 public static void main(String[] args) {
 int[] Sortit;
 	if (args[Sortit.length-1]==up) {
	Arrays.sort(Sortit);
	} else if (args[Sortit.length-1]==down) {
	Arrays.sort(Sortit, Collections.reverseOrder());
	} else if (args[Sortit.length-1]!=up & down) {
	Arrays.sort(Sortit)}
	
	int i=0;
	for(i=0;i==Sortit.length;i++){
	System.getPropery(Sortit[i]);
	System.out.println(Sortit[i]);
	}
}
}
	
	